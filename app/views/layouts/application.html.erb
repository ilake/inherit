<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title><%= h(yield(:title)) %></title>
  <%= stylesheet_link_tag 'scaffold', 'application' %>
  <%= javascript_include_tag 'jquery' %>
  <%= yield(:head) %>
</head>
<body>
  <div id="user_nav">
    <%= link_to_unless_current 'Home', root_url %>
    <% if user_signed_in? %>
      Signed in as <%= current_user.username %>
      <%= link_to "Sign out", destroy_user_session_path %>

      <%= link_to "#{current_user.username} goals", user_goals_path(current_user)%>
      <%= link_to "#{current_user.username} experiences", user_experiences_path(current_user)%>
    <% else %>
      <%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>
    <% end %>
  </div>

  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <% end %>


<%= yield %>

</body>
</html>
