<h2><%= t'user.fans' %></h2>
<ul>
  <% @users.each do |u| %>
    <li class='exp_author'>
      <%= link_to_user_home(u) %>
      <%= truncate_u(Sanitize.clean(u.profile.intro), 30) %>
    </li>
<% end %>
</ul>
